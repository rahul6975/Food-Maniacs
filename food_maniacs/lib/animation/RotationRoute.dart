import 'package:flutter/material.dart';

class RotationRoute extends StatelessWidget {
  RotationRoute({Key key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
